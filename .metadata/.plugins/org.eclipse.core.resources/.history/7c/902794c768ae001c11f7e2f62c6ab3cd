<!DOCTYPE html>
<html xmlns:th="http://www.thymleaf.org">>

<!--
	컨텐츠를 전달할 요소를 css의 ~{:: 선택자} 형식으로 사용
	layout03의 템플릿으로 "선택자" 부분을 전달하게 됨
	id는 #, class는 . 으로 선택  
-->

<th:block th:replace="~{/include/layout03 :: 함수( ~{:: #first} ) }">
		
		<div id="first">
			first부분
		</div>
		
</th:block>

</html>